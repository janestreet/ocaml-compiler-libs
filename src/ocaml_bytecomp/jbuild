(library
 ((name        ocaml_bytecomp)
  (public_name ocaml-compiler-libs.bytecomp)
  (flags (:standard -safe-string))
  (libraries (compiler-libs.bytecomp))
  (preprocess no_preprocessing)))

(rule
 ((targets (ocaml_bytecomp.ml))
  (deps    (../gen/gen.exe))
  (action  (run ${<}
            -ocaml-where ${ocaml_where} ocamlbytecomp
            -o ${@}))))

(jbuild_version 1)
